"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.nthIndex = exports.getIndexes = exports.getStringFromTag = exports.removeTag = void 0;
var removeTag = function (input, tag) {
    var regex = new RegExp("/<{tag}(.*)>(.*)</" + tag + ">/gi");
    return input.replace(regex, "");
};
exports.removeTag = removeTag;
var getStringFromTag = function (input, tag) {
    var regex = new RegExp("/<" + tag + "(.*?)>(.+?)</" + tag + ">/gi");
    var matches = regex.exec(input);
    if (!matches)
        return [];
    return matches;
};
exports.getStringFromTag = getStringFromTag;
var getIndexes = function (source, find) {
    var result = [];
    var i = 0;
    while (i < source.length) {
        if (source.substring(i, i + find.length) === find) {
            result.push(i);
            i += find.length;
        }
        else {
            i++;
        }
    }
    return result;
};
exports.getIndexes = getIndexes;
var nthIndex = function (source, find, nth) {
    var result = (0, exports.getIndexes)(source, find);
    return result[nth];
};
exports.nthIndex = nthIndex;
//# sourceMappingURL=strings.js.map